services:
  api:
    build:
      context: .
    container_name: marathon-bot-api
    ports:
      - "${PORT:-8000}:8000"
    environment:
      APP_ENV: ${APP_ENV:-local}
      PORT: 8000
      DATABASE_URL: ${DATABASE_URL:-sqlite:///./local.db}
      SLACK_BOT_TOKEN: ${SLACK_BOT_TOKEN:-}
      SLACK_SIGNING_SECRET: ${SLACK_SIGNING_SECRET:-}
      APP_BASE_URL: ${APP_BASE_URL:-http://127.0.0.1:8000}
      JOB_SECRET: ${JOB_SECRET:-change-me}
      TURSO_DATABASE_URL: ${TURSO_DATABASE_URL:-}
      TURSO_AUTH_TOKEN: ${TURSO_AUTH_TOKEN:-}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      ENABLE_LLM_IMAGE_ANALYSIS: ${ENABLE_LLM_IMAGE_ANALYSIS:-true}
      OPENAI_VISION_MODEL: ${OPENAI_VISION_MODEL:-gpt-4o-mini}
      OPENAI_VISION_MAX_IMAGES: ${OPENAI_VISION_MAX_IMAGES:-3}
    volumes:
      - .:/app
    command: uvicorn backend.app.main:app --host 0.0.0.0 --port 8000 --reload
